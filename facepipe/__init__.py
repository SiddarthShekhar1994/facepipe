"""facepipe: a local face detection and recognition pipeline.

Stage interfaces live in `facepipe.interfaces`; the types that cross them in
`facepipe.types`. Nothing in this package imports a web framework.
"""
